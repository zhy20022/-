# 🎮 游戏快速启动指南

## ✅ 当前状态检查

运行以下命令检查环境：
```bash
python check_environment.py
```

---

## 📋 环境准备清单

### 1. ✅ Python 3.8+ （已完成）
- 当前版本：Python 3.13.1
- 状态：✓ 已安装

### 2. ✅ Python 依赖包 （已完成）
- Flask, SQLAlchemy, psycopg2-binary 等
- 状态：✓ 已安装（包括 APScheduler）

### 3. ❌ PostgreSQL 数据库 （需要安装/配置）
- 状态：✗ 未安装或未启动

**安装步骤：**
1. 下载 PostgreSQL：https://www.postgresql.org/download/windows/
2. 安装时记住设置的密码（默认用户名：`postgres`）
3. 安装完成后，打开 **pgAdmin** 或命令行工具
4. 创建数据库：
   ```sql
   CREATE DATABASE gamedb;
   ```
5. 启动 PostgreSQL 服务：
   - Windows：打开"服务"管理器，找到 "PostgreSQL" 服务，右键"启动"
   - 或使用命令：`net start postgresql-x64-XX`（XX 是版本号）

**验证安装：**
```bash
# 测试连接（需要先安装 psql 命令行工具）
psql -U postgres -d gamedb
```

### 4. ❌ Node.js 16+ （需要安装）
- 状态：✗ 未安装

**安装步骤：**
1. 访问：https://nodejs.org/
2. 下载 **LTS 版本**（推荐）
3. 安装时选择"添加到 PATH"
4. 安装完成后，**重启命令行窗口**
5. 验证安装：
   ```bash
   node --version
   npm --version
   ```

---

## 🚀 启动步骤

### 第一步：确保 PostgreSQL 运行

**Windows：**
1. 按 `Win + R`，输入 `services.msc`
2. 找到 "PostgreSQL" 服务
3. 如果状态不是"正在运行"，右键点击"启动"

**验证数据库：**
```bash
python test_database.py
```
如果看到 `[OK] 数据库连接成功`，说明数据库配置正确。

### 第二步：安装前端依赖

```bash
cd web
npm install
```

**Windows PowerShell：**
```powershell
cd web; if ($?) { npm install }
```

### 第三步：启动后端服务器

**打开第一个命令行窗口：**
```bash
python run_server.py
```

看到以下输出表示成功：
```
启动服务器: http://0.0.0.0:5000
定时任务调度器已启动（活动轮换检查：每30分钟）
 * Running on http://127.0.0.1:5000
```

**保持这个窗口打开！**

### 第四步：启动前端开发服务器

**打开第二个命令行窗口：**
```bash
cd web
npm run dev
```

看到以下输出表示成功：
```
  VITE v5.x.x  ready in xxx ms
  ➜  Local:   http://localhost:5173/
```

**保持这个窗口打开！**

### 第五步：访问游戏

打开浏览器，访问：**http://localhost:5173/**

---

## ⚠️ 常见问题

### 问题1：PostgreSQL 连接失败

**错误信息：** `Connection refused` 或 `无法连接到数据库`

**解决方案：**
1. 确认 PostgreSQL 服务已启动（见"第一步"）
2. 确认数据库 `gamedb` 已创建
3. 检查用户名和密码是否正确
4. 如果使用自定义配置，创建 `.env` 文件：
   ```env
   DATABASE_URL=postgresql://用户名:密码@localhost:5432/gamedb
   ```

### 问题2：Node.js 命令不存在

**解决方案：**
1. 确认已安装 Node.js
2. **重启命令行窗口**（重要！）
3. 如果还是不行，检查环境变量 PATH

### 问题3：端口被占用

**后端端口（5000）被占用：**
- 修改 `run_server.py` 或设置环境变量 `PORT=其他端口`

**前端端口（5173）被占用：**
- Vite 会自动使用下一个可用端口

### 问题4：前端无法连接后端

**解决方案：**
1. 确认后端已启动（http://localhost:5000）
2. 打开浏览器控制台（F12）查看错误
3. 检查 CORS 配置

---

## 📝 快速检查命令

```bash
# 检查 Python 环境
python --version

# 检查 Python 依赖
pip list | findstr "flask sqlalchemy psycopg2"

# 检查 Node.js（需要先安装）
node --version
npm --version

# 检查数据库连接
python test_database.py

# 运行完整环境检查
python check_environment.py
```

---

## 🎯 下一步

1. ✅ 安装 PostgreSQL 并创建数据库
2. ✅ 安装 Node.js
3. ✅ 运行 `python check_environment.py` 确认所有检查通过
4. ✅ 按照"启动步骤"启动游戏

---

**需要帮助？** 查看 `docs/游戏启动指南.md` 获取更详细的说明。

