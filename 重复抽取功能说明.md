# 重复抽取功能说明

## 当前实现

当前的抽卡系统**已经支持重复抽取角色**，逻辑如下：

### 1. 抽取流程

1. **从池子中随机抽取角色**
   - `pool.get_random_character()` 方法会从池子中随机抽取一个角色
   - **不会排除已拥有的角色**，所有角色都可以被抽取到

2. **判断是否为重复角色**
   - 检查 `character.character_id in self.owned_characters`
   - 如果角色ID在已拥有列表中，则标记为重复

3. **处理重复角色**
   - **如果是重复角色**：转化为精华（每个重复角色获得1个精华）
   - **如果不是重复角色**：添加到已拥有列表，保存到数据库

### 2. 代码位置

- `src/rewards/gacha.py` 第198-217行：`GachaSystem.pull()` 方法
- `src/server/routes.py` 第586-633行：处理重复角色的逻辑

### 3. 功能确认

- ✅ 可以重复抽到相同角色
- ✅ 重复角色会自动转化为精华
- ✅ 新角色会保存到数据库
- ✅ 重复角色不会重复保存到数据库

## 如果功能不工作，可能的原因

1. **已拥有角色列表未正确加载**
   - 检查 `_load_battle_soul_data` 是否正确加载了已拥有角色

2. **角色ID匹配问题**
   - 预定义角色的 `character_id` 可能不匹配
   - 检查 `character_id` 的生成逻辑

3. **数据库查询问题**
   - 检查已拥有角色的数据库查询是否正常

## 测试建议

1. 抽取一个角色
2. 再次抽取，应该能抽到相同角色
3. 查看返回结果中的 `is_duplicate` 字段
4. 查看精华数量是否增加



