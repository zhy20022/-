# 快速启动服务器指南

## 当前状态
- ✅ 后端服务器：已在端口 **5000** 运行
- ❌ 前端服务器：需要手动启动

## 启动前端服务器

### 方法一：使用命令行（推荐）

1. 打开一个新的 PowerShell 或 CMD 窗口
2. 进入项目目录：
   ```powershell
   cd C:\Users\12107\Desktop\4\Gamer\web
   ```
3. 启动前端服务器：
   ```powershell
   npm run dev
   ```

### 方法二：使用批处理文件

双击运行 `启动游戏.bat` 文件

## 访问地址

前端服务器启动后，访问：
- **http://localhost:3000** （根据 vite.config.ts 配置）

如果端口3000被占用，Vite会自动使用下一个可用端口（如3001、3002等），请查看启动时的输出信息。

## 检查服务器状态

在浏览器中访问以下地址测试：

1. **后端API测试**：http://localhost:5000/api/player/info
   - 应该返回 JSON：`{"success":false,"message":"未登录"}`
   - 如果能看到这个响应，说明后端正常

2. **前端页面**：http://localhost:3000
   - 应该看到游戏登录页面

## 如果仍然无法访问

1. 检查防火墙是否阻止了端口
2. 确认端口没有被其他程序占用
3. 查看前端服务器窗口的错误信息












