# 如何查看浏览器控制台错误信息

## 步骤1：打开浏览器开发者工具

### Chrome/Edge 浏览器：
1. 按 **F12** 键（或 **Ctrl + Shift + I**）
2. 或者：右键点击页面 → 选择"检查"（Inspect）

### Firefox 浏览器：
1. 按 **F12** 键（或 **Ctrl + Shift + I**）
2. 或者：右键点击页面 → 选择"检查元素"

### Safari 浏览器（Mac）：
1. 首先需要启用开发者菜单：
   - Safari → 偏好设置 → 高级 → 勾选"在菜单栏中显示开发菜单"
2. 然后按 **Cmd + Option + I**

## 步骤2：切换到 Console（控制台）标签
- 开发者工具打开后，点击顶部的 **"Console"** 标签

## 步骤3：查看错误信息
1. 在控制台中，错误信息通常以红色显示
2. 尝试抽取角色
3. 查看控制台中是否有新的错误信息出现

## 步骤4：查看 Network（网络）标签（重要！）
1. 点击开发者工具顶部的 **"Network"** 标签
2. 尝试抽取角色
3. 在请求列表中查找 **`/api/gacha/pull`** 请求
4. 点击这个请求
5. 查看右侧的详细信息：
   - **Headers（请求头）**：查看请求参数
   - **Preview（预览）** 或 **Response（响应）**：查看服务器返回的数据
   - **Status（状态）**：查看HTTP状态码（200=成功，400/500=错误）

## 示例：查看API响应
1. 找到 `/api/gacha/pull` 请求
2. 点击它
3. 查看 **Response** 或 **Preview** 标签
4. 应该能看到类似这样的JSON：
   ```json
   {
     "success": false,
     "message": "抽取失败: 具体错误信息"
   }
   ```
5. **复制这个完整的JSON响应**

## 常见错误信息位置：
- **红色错误文本**：在Console标签中
- **HTTP错误**：在Network标签中，状态码不是200
- **API错误消息**：在Network → Response/Preview中

## 提示：
- 如果控制台信息太多，可以点击清除按钮（🚫图标）清空，然后重新操作
- 确保在操作前打开开发者工具，这样可以看到完整的错误流程



