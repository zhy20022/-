# 📦 环境安装指导

## 🚀 方式一：自动安装（推荐）

### Windows 10/11 用户

1. **右键点击 `自动安装环境.ps1`**
2. **选择"以管理员身份运行"**
3. 脚本会自动安装 Node.js 和 PostgreSQL

**注意：** 需要 Windows 10/11 系统，且需要管理员权限。

---

## 🛠️ 方式二：手动安装（详细步骤）

### 1. 安装 Node.js

#### 步骤：
1. **访问下载页面**：https://nodejs.org/
2. **下载 LTS 版本**（推荐，稳定版）
3. **运行安装程序**
4. **重要：勾选"Add to PATH"选项**
5. **点击"Install"完成安装**
6. **重启命令行窗口**

#### 验证安装：
打开新的命令行窗口，运行：
```bash
node --version
npm --version
```
如果显示版本号，说明安装成功。

---

### 2. 安装 PostgreSQL

#### 步骤：
1. **访问下载页面**：https://www.postgresql.org/download/windows/
2. **点击"Download the installer"**
3. **选择最新版本下载**
4. **运行安装程序**
5. **安装选项**：
   - 安装路径：默认即可
   - 数据目录：默认即可
   - 密码：**请记住这个密码！**（默认用户是 `postgres`）
   - 端口：默认 5432
   - 地区：选择 `Chinese, China`
6. **完成安装**

#### 创建数据库：
安装完成后，有两种方式创建数据库：

**方式 A：使用 pgAdmin（图形界面）**
1. 打开 **pgAdmin**（安装时一起安装的）
2. 连接到服务器（输入安装时设置的密码）
3. 右键点击 "Databases" → "Create" → "Database"
4. 数据库名称输入：`gamedb`
5. 点击 "Save"

**方式 B：使用命令行**
1. 打开命令行
2. 运行：
```bash
psql -U postgres
```
3. 输入安装时设置的密码
4. 运行：
```sql
CREATE DATABASE gamedb;
```
5. 退出：`\q`

#### 启动 PostgreSQL 服务：
**Windows：**
1. 按 `Win + R`，输入 `services.msc`
2. 找到 "postgresql-x64-XX"（XX 是版本号）
3. 如果状态不是"正在运行"，右键点击"启动"

或者使用命令行（管理员权限）：
```bash
net start postgresql-x64-16
```
（版本号可能不同）

#### 验证安装：
```bash
psql -U postgres -d gamedb
```
如果能连接，说明安装成功。

---

## ✅ 验证安装

运行环境检查脚本：
```bash
python check_environment.py
```

所有项目显示 `[OK] 通过` 即可。

---

## 🎮 启动游戏

安装完成后，直接双击 `启动游戏.bat` 即可！

---

## ❓ 常见问题

### Node.js 安装后命令不可用？
- **解决方案**：重启命令行窗口
- 如果还是不行，检查环境变量 PATH 是否包含 Node.js 安装路径

### PostgreSQL 连接失败？
- **检查服务是否启动**：打开服务管理器，确认 PostgreSQL 服务正在运行
- **检查数据库是否存在**：使用 pgAdmin 或命令行检查
- **检查密码是否正确**：确认使用的是安装时设置的密码

### 需要修改数据库配置？
创建 `.env` 文件（项目根目录）：
```env
DATABASE_URL=postgresql://postgres:你的密码@localhost:5432/gamedb
```

---

## 📞 需要帮助？

如果遇到问题：
1. 查看错误信息
2. 运行 `python check_environment.py` 查看具体问题
3. 查看 `docs/游戏启动指南.md` 获取详细帮助

