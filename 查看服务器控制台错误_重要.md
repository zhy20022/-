# ⚠️ 重要：如何查看服务器控制台错误

## 您遇到了500错误，需要查看服务器控制台获取具体错误信息

### 操作步骤：

1. **找到后端服务器窗口**
   - 运行 `启动游戏.bat` 后，会打开两个窗口
   - 其中一个显示Python运行信息，标题可能是：
     - "后端服务器"
     - 或者显示Python路径和代码
   
2. **查看错误信息**
   - 在这个窗口中，应该能看到红色的错误信息
   - 错误信息格式类似：
     ```
     Traceback (most recent call last):
       File "src/server/routes.py", line 571, in gacha_pull
         results = gacha.pull(pool, pull_count, player.gold)
       File "...", line XX, in ...
         ...
     AttributeError: 'NoneType' object has no attribute 'xxx'
     ```
   
3. **复制错误信息**
   - 在窗口中右键 → 选择"标记"
   - 用鼠标选中错误文本（从Traceback开始到错误类型结束）
   - 按 Enter 键复制
   - 或者直接截图整个窗口

### 如果没有看到错误：
- 确保服务器正在运行
- 尝试再次点击"单抽"按钮
- 立即查看服务器窗口（错误信息可能会被新的输出覆盖）
- 检查窗口是否有滚动条，向上滚动查看更早的错误

### 请将错误信息提供给我，这样我可以精确修复问题！



