# 完整调试指南：如何获取错误信息

## 概述
当游戏出现"角色抽取失败"错误时，需要收集以下3类信息来诊断问题。

---

## 1. 服务器控制台错误信息

### 操作步骤：

1. **找到后端服务器窗口**
   - 运行 `启动游戏.bat` 后，会打开两个窗口
   - 其中一个标题为"后端服务器"或显示Python运行的窗口
   - 如果没有看到，重新运行 `启动游戏.bat`

2. **查看错误输出**
   - 在服务器窗口中，错误信息通常显示为红色文本
   - 错误信息包含：
     ```
     Traceback (most recent call last):
       File "路径", line 行号, in 函数名
         代码行
     错误类型: 错误描述
     ```

3. **复制错误信息**
   - 在窗口中右键点击 → 标记
   - 选中错误信息文本
   - 按 Enter 复制
   - 或者直接截图整个窗口

### 如果没有看到错误：
- 确保服务器正在运行
- 尝试再次抽取，观察窗口输出
- 查看窗口是否有滚动条，错误可能在更早的位置

---

## 2. 浏览器控制台错误信息

### 操作步骤：

#### 第一步：打开开发者工具

**方法1（推荐）：**
- 按键盘上的 **F12** 键

**方法2：**
- 按 **Ctrl + Shift + I**（Windows/Linux）
- 或 **Cmd + Option + I**（Mac）

**方法3：**
- 右键点击页面空白处
- 选择"检查"或"Inspect Element"

#### 第二步：查看Console标签

1. 开发者工具打开后，默认会在Console标签
2. 如果不在，点击顶部的 **"Console"** 标签
3. 查看是否有红色错误信息

#### 第三步：清空控制台（可选）

1. 点击控制台左上角的清除按钮（🚫图标）
2. 这样可以看到新的错误信息，不会被旧的干扰

#### 第四步：执行操作并查看错误

1. 在控制台打开的情况下，尝试抽取角色
2. 观察控制台是否出现新的错误信息
3. 错误信息通常以红色显示，格式类似：
   ```
   Error: 错误描述
   at 函数名 (文件路径:行号)
   ```

---

## 3. API响应中的具体错误消息（最重要！）

### 操作步骤：

#### 第一步：打开开发者工具的Network标签

1. 在开发者工具中（F12已打开），点击顶部的 **"Network"** 标签
2. 如果看不到Network标签，可能需要调整开发者工具窗口大小

#### 第二步：清空网络请求列表（可选）

1. 点击Network标签左上角的清除按钮（🚫图标）
2. 这样只看到新的请求

#### 第三步：执行抽取操作

1. 在Network标签打开的情况下，点击游戏中的"单抽"或"10连"按钮
2. 观察Network标签中的请求列表

#### 第四步：找到API请求

1. 在请求列表中，查找名为 **`gacha/pull`** 或 **`pull`** 的请求
2. 请求列表通常按时间排序，最新的在最上面
3. 请求的URL应该类似：`http://localhost:5000/api/gacha/pull`

#### 第五步：查看请求状态

1. 查看请求的 **Status（状态）** 列
   - **200** = 成功（但可能逻辑错误）
   - **400/500** = 服务器错误
   - **红色显示** = 请求失败

#### 第六步：查看响应内容

1. **点击** `/api/gacha/pull` 这个请求
2. 在右侧会显示详细信息面板
3. 点击 **"Response"** 或 **"Preview"** 标签
4. 查看服务器返回的JSON数据

#### 预期看到的内容：

**如果成功但逻辑错误：**
```json
{
  "success": false,
  "message": "具体的错误消息"
}
```

**如果服务器错误：**
```json
{
  "success": false,
  "message": "抽取失败: 错误描述"
}
```

#### 第七步：复制错误信息

1. 选中Response/Preview中的JSON文本
2. **完整复制**整个JSON对象
3. 或者截图保存

---

## 快速检查清单

在报告错误前，请确认您有：

- [ ] **服务器控制台输出**：后端窗口中的错误堆栈（如果有）
- [ ] **浏览器Console错误**：F12 → Console标签中的红色错误
- [ ] **API响应内容**：F12 → Network → `/api/gacha/pull` → Response中的JSON

---

## 如何提供信息

将收集到的信息以以下格式提供：

```
1. 服务器控制台错误：
（粘贴或描述错误信息）

2. 浏览器Console错误：
（粘贴或描述错误信息）

3. API响应内容：
（粘贴完整的JSON响应）
```

---

## 常见问题

**Q: 看不到Network标签？**
A: 尝试调整开发者工具窗口大小，或者点击三个点菜单查看所有标签

**Q: Network中没有gacha/pull请求？**
A: 确保在Network标签打开后才点击抽取按钮，请求可能被清空了

**Q: 服务器窗口已关闭？**
A: 重新运行 `启动游戏.bat`，然后再次尝试

**Q: 看不到任何错误？**
A: 可能错误已经被处理，查看Network中的响应状态和内容

---

## 提示

- **最重要**：第3项（API响应）通常包含最详细的错误信息
- 如果时间有限，优先查看Network标签中的API响应
- 截图也是一种很好的方式，可以完整保存错误信息



