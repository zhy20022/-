# 游戏无法运行问题解决方案

## 问题诊断结果

根据诊断，发现以下情况：
- ✅ **后端服务器**：正常运行在端口 5000
- ❌ **前端服务器**：未在端口 3000 运行（这是主要问题）
- ✅ **数据库**：连接正常（使用SQLite）

## 解决方案

### 步骤1：确认前端服务器是否正在运行

请检查您的前端服务器启动窗口，应该看到类似以下信息：
```
  VITE v5.x.x  ready in xxx ms
  ➜  Local:   http://localhost:3000/
```

如果没有看到这个消息，说明前端服务器启动失败或未启动。

### 步骤2：启动前端服务器

1. **打开一个新的命令行窗口**（不要关闭现有的后端服务器窗口）

2. **进入前端目录**：
   ```bash
   cd web
   ```

3. **启动前端服务器**：
   ```bash
   npm run dev
   ```

4. **等待启动完成**，应该看到：
   ```
   VITE v5.x.x  ready in xxx ms
   ➜  Local:   http://localhost:3000/
   ```

### 步骤3：访问游戏

前端服务器启动后，在浏览器中访问：
**http://localhost:3000**

## 常见问题排查

### 问题1：npm run dev 报错 "npm不是内部或外部命令"
**解决方法**：
- 确认已安装 Node.js：访问 https://nodejs.org/ 下载安装
- **重启命令行窗口**（重要！）
- 检查 Node.js 是否正确安装：运行 `node --version` 和 `npm --version`

### 问题2：npm run dev 报错 "无法找到模块" 或 "依赖缺失"
**解决方法**：
```bash
cd web
npm install
```
然后重新运行 `npm run dev`

### 问题3：端口 3000 已被占用
**解决方法**：
- Vite 会自动尝试下一个可用端口（3001, 3002等）
- 查看启动信息中的实际端口号
- 使用实际显示的端口访问游戏

### 问题4：前端显示但无法连接后端
**检查项**：
1. 确认后端服务器正在运行（端口 5000）
2. 打开浏览器控制台（F12），查看是否有错误信息
3. 确认浏览器访问的地址是 **http://localhost:3000**（不是其他端口）

## 正确的启动流程

### 方式1：手动启动（推荐用于调试）

**窗口1 - 后端服务器**：
```bash
python run_server.py
```
应该看到：
```
启动服务器: http://0.0.0.0:5000
* Running on http://127.0.0.1:5000
```

**窗口2 - 前端服务器**：
```bash
cd web
npm run dev
```
应该看到：
```
  VITE v5.x.x  ready in xxx ms
  ➜  Local:   http://localhost:3000/
```

**然后访问**：http://localhost:3000

### 方式2：使用启动脚本

如果项目中有 `启动游戏.bat` 或 `启动游戏.py`，可以尝试使用它们。

## 验证所有服务都在运行

运行以下命令检查端口：

**Windows PowerShell**：
```powershell
netstat -ano | findstr ":3000 :5000"
```

应该看到两个端口都在 LISTENING 状态。

## 如果问题仍然存在

1. **检查浏览器控制台**（按F12）：
   - 查看是否有JavaScript错误
   - 查看Network标签，看API请求是否成功

2. **检查后端日志**：
   - 查看后端服务器窗口的输出
   - 看是否有错误信息

3. **尝试清除缓存**：
   - 在浏览器中按 Ctrl+Shift+Delete 清除缓存
   - 或使用隐私模式访问游戏

4. **重启所有服务**：
   - 关闭所有服务器窗口
   - 重新启动后端服务器
   - 重新启动前端服务器

## 联系支持

如果以上方法都无法解决问题，请提供以下信息：
- 浏览器控制台的错误信息（F12）
- 后端服务器的错误日志
- 前端服务器的启动输出
- 操作系统版本











