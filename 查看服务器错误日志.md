# 如何查看服务器控制台错误信息

## 方法1：查看后端服务器窗口
1. 找到运行后端服务器的命令行窗口（标题通常是"后端服务器"或显示 Python 运行的窗口）
2. 查看窗口中的红色错误信息或堆栈跟踪（traceback）
3. 错误信息通常会显示：
   - 错误类型（如 AttributeError, KeyError, ValueError 等）
   - 错误发生的文件和行号
   - 完整的错误堆栈

## 方法2：如果窗口已关闭
1. 重新运行 `启动游戏.bat`
2. 等待后端服务器启动
3. 在服务器运行期间尝试抽取角色
4. 立即查看后端服务器窗口中的输出

## 示例错误信息格式：
```
Traceback (most recent call last):
  File "src/server/routes.py", line 571, in gacha_pull
    results = gacha.pull(pool, pull_count, player.gold)
  File "src/rewards/gacha.py", line 199, in pull
    character = pool.get_random_character()
AttributeError: 'NoneType' object has no attribute 'get_random_character'
```

## 重要提示：
- 如果看到错误，请**完整复制**错误信息（包括所有行）
- 特别注意错误类型和文件名、行号
- 错误信息通常以红色文本显示



